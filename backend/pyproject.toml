[project]
name = "backend"
version = "0.1.0"
description = "Yorozuya's only backend"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "datetime>=6.0",
    "dotenv>=0.9.9",
    "fastapi>=0.138.0",
    "httpx>=0.28.1",
    "uvicorn>=0.49.0",
]
